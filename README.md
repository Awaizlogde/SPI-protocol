# SPI-protocol
This project implements a complete SPI (Serial Peripheral Interface) system protocol in Verilog, including both Master and Slave modules, along with a testbench for functional verification.  The design is based on an FSM architecture and supports configurable SPI modes using CPOL and CPHA.
